package render

// Package render will contain the STL preview rendering adapter.
// Keep this package dependency-light while the basic web server is being scaffolded.

const DefaultScriptPath = "./render_stl.sh"
